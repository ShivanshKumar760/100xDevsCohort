const array=[1,2,3];//array of integer
//iterating through the array using the for..of loop
for(let val/*val is a variable iterator which will carry the value array[index]
ie val=array[index]*/ of array)
{
    //val=array[currentValueIndex]
    console.log(val);//console.log(array[i])
}